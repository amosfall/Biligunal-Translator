{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }],
  "functions": {
    "api/translate-stream.ts": { "maxDuration": 300 },
    "api/translate.ts": { "maxDuration": 300 }
  }
}
